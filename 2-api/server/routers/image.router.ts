// import Router from 'koa-router';
// import { deleteImage } from '../controllers/user/profile-image/delete-profile-image.controller';
// import { getImages } from '../controllers/get-images.controller';
// import { getSignedUrlForUploadingImage } from '../controllers/user/profile-image/get-signed-url-for-uploading-profile-image.controller';
// import { storeUploadedImageInformation } from '../controllers/store-uploaded-profile-image-information.controller';

// const imageRouter = new Router();

// imageRouter.get('/images', getImages);

// imageRouter.post(
//   '/get-signed-url-for-uploading-image',
//   getSignedUrlForUploadingImage
// );
// imageRouter.post(
//   '/store-uploaded-image-information',
//   storeUploadedImageInformation
// );
// imageRouter.delete('/delete-image/:imageId', deleteImage);

// export { imageRouter };
